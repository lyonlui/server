package session
/*
import (
  "fmt"
)

var GlobalSessions *session.Manager

func init() {
	var config session.ManagerConfig
	config.CookieName = "GameID"
	config.Gclifetime = 50
	config.ProviderConfig = "root:HelloWorld!@/qpaccountsdb"
	config.SessionIDLength = 16

	GlobalSessions, _ = session.NewManager("mysql", &config)
	go GlobalSessions.GC()
}


func main() {

	session, err := test.GlobalSessions.CreatNewSession()

	if err != nil {
		fmt.Println(err)
		return

	}

	sid := session.SessionID()

	fmt.Println(sid)

	sum := 0
	for sum < 1 {

	}
}
*/
